<fieldset>
  <legend><%= I18n.t(:"signup.bountyhill")%></legend>
  <%= render 'shared/error_messages', :object => @user, :scope => "signup" %>

  <div class="control-group <%= error_class_for(@user, :name) %>">
    <%= form.label :name, :class => "control-label" %> 
    <div class="controls">
      <%= form.text_field :name, :class => "input-xlarge" %> 
      <%= error_message_for(@user, :name) %>
    </div>
  </div>
  
  <div class="control-group <%= error_class_for(@user, :email) %>">
    <%= form.label :email, :class => "control-label" %> 
    <div class="controls">
      <%= form.text_field :email, :class => "input-xlarge" %>
      <%= error_message_for(@user, :email) %>
    </div>
  </div>
  
  <div class="control-group <%= error_class_for(@user, :password) %>">
    <%= form.label :password, :class => "control-label" %> 
    <div class="controls">
      <%= form.password_field :password, :class => "input-xlarge" %>
      <%= error_message_for(@user, :password) %>
    </div>
  </div>
  
  <div class="control-group <%= error_class_for(@user, :password_confirmation) %>">
    <%= form.label :password_confirmation, :class => "control-label" %> 
    <div class="controls">
      <%= form.password_field :password_confirmation, :class => "input-xlarge" %>
      <%= error_message_for(@user, :password_confirmation) %>
    </div>
  </div>
  
  <div class="form-actions">
    <%#= link_to I18n.t(:cancel), root_path, :class => "btn btn-mini" %>
    <%= form.submit I18n.t(:sign_up), :class => "btn btn-primary btn" %>
  </div>
</fieldset>