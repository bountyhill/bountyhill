<%= header_ribbon(
    header_action_button_for_user(@user),
    interaction_buttons(
      delete_user_button(@user),
      edit_user_button(@user),
      follow_user_button(@user)
    )
  ) %>

<div class="row bg-main">
  <div class="span2">&nbsp;</div>
  <div class="user sheet span8">
    <div class="inner">
      <%= render_user_section(@user, "card") %>
      <%= render_user_section(@user, "badge") %>
      <%= render_user_section(@user, "activities") %>
    </div>
  </div>
  <div class="span2">&nbsp;</div>
</div>
