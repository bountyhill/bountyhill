<% if (name = facebook && facebook.name).present? %>
  <div class="content-box">
    <%= privacy_icon(:facebook) %>
    <div class='label'><%= I18n.t("user.box.facebook.label") %>:</div> <%= name %>
  </div>
<% else %>
 <%= I18n.t("user.box.facebook.text") %>
<% end %>