<%= form_for(identity, :url => "/auth/linkedin/init") do |form| %>
  <% if current_user %>
    <p><%= I18n.t("identity/linkedin.form.new.legend") %></p>
  <% else %>
    <p><%= I18n.t("identity/linkedin.form.signin.legend") %></p>
  <% end %>

  <%= form.error_messages @error if @error %>
  <%#= hidden_field_tag :req, :linkedin %>

  <% unless current_user || returning_user? %>
    <p><%= I18n.t("identity.form.commercial") %></p>
    <%= form.control_group :commercial, :check_box %>
  <% end %>

  <% if current_user && !identity_requested?(:linkedin) %>
    <%= form.actions "#", :"data-dismiss" => "modal" %>
  <% else %>
    <% unless current_user || returning_user? %>
      <p><%= I18n.t("identity.form.agree_to_terms")%></p>
      <%= form.agree_to_terms(:id => "agree_to_terms_linkedin") %>
    <% end %>
    
    <div class="buttons">
      <%= link_to_cancel_signin %>
      <%= form.submit I18n.t("button.signin"),
        :name => "signin_linkedin",
        :id => "signin_linkedin",
        :class => "btn btn-primary btn-inverse" %>
    </div>
    
    <%= signin_button_toggle_javascript(:linkedin) %>
  <% end %>
<% end %>
