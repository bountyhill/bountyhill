<%= form_for(identity, :url => "/auth/xing/init") do |form| %>
  <% if current_user %>
    <p><%= I18n.t("identity/xing.form.new.legend") %></p>
  <% else %>
    <p><%= I18n.t("identity/xing.form.signin.legend") %></p>
  <% end %>

  <%= form.error_messages @error if @error %>
  <%#= hidden_field_tag :req, :xing %>

  <%= form.control_group :commercial, :check_box %>

  <% if current_user && !identity_requested?(:xing) %>
    <%= form.actions "#", :"data-dismiss" => "modal" %>
  <% else %>
    <% unless current_user %>
      <p><%= I18n.t("identity.form.agree_to_terms")%></p>
      <%= form.agree_to_terms(:id => "agree_to_terms_xing") %>
    <% end %>
    
    <div class="buttons">
      <%= link_to_cancel_signin %>
      <%= form.submit I18n.t("button.signin"),
        :name => "signin_xing",
        :id => "signin_xing",
        :class => "btn btn-primary btn-inverse",
        :disabled => "disabled" %>
    </div>
  <% end %>
<% end %>

<script type="text/javascript" charset="utf-8">
  $("#agree_to_terms_xing").change(function() {
    if($(this).is(':checked'))
      $("#signin_xing").removeAttr("disabled");
    else
      $("#signin_xing").attr("disabled", "disabled");
  });
</script>