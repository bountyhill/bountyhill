<ul class="fp-slides">
<% thumbnails, images = offer.images(:width => 100, :height => 100), offer.images %>

<% thumbnails.zip(images).each do |thumbnail, image| %>
  <li>
    <%= link_to image_tag(thumbnail), image, :target => "_blank" %>
  </li>
<% end %>
</ul>
