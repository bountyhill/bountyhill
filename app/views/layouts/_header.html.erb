<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <!-- .btn-navbar is used as the toggle for collapsed navbar content -->
      <button type="button"class="btn btn-navbar pull-right" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'bountyhill', root_path, :class => "brand span2" %>
      <form class="navbar-search" action="">
        <input type="text" class="search-query input-xlarge" placeholder="<%= I18n.t(:"request.text") %>">
      </form>
      <div class="nav-collapse">
        <ul class="nav">
          <li class="divider"></li>
          <% %w(quests).each do |nav_item| %>
            <li><%= link_to I18n.t(nav_item), self.send("#{nav_item}_path") %></li>
          <% end %>
          <li class="divider"></li>
        </ul>
        <ul class="nav pull-right">
          <% if signed_in? %>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%= current_user.name %> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to I18n.t(:profile), current_user %></li>
                <li><%= link_to I18n.t(:settings), '#' %></li>
                <li class="divider"></li>
                <li><%= link_to I18n.t(:sign_out), signout_path, method: "delete" %></li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to I18n.t(:sign_up), signup_path %></li>
            <li><%= link_to I18n.t(:sign_in), signin_path %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>
<% flash.each do |key, value| %>
  <div class="flash alert alert-<%= key %>">
    <a class="close" data-dismiss="alert" href="#">×</a>
    <%= value %>
  </div>
<% end %>
