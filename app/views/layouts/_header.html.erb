<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="brand">
      <%= link_to image_tag(asset_path('logo.png'), :class => 'logo'), root_path %>
      <%= link_to image_tag(asset_path('logo_small.png'), :class => 'logo_small'), root_path %>
    </div>
    <%= render_navigation_items :header_center %>
    <%= user_navigation %>
  </div>
</div>
